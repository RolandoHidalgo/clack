{
  "name": "@skidrow/clack-fork-root",
  "private": true,
  "type": "module",
  "publishConfig": { "access": "public" },
  "scripts": {
    "stub": "pnpm -r run build --stub",
    "build": "pnpm --filter \"@skidrow/clack-*\" run build",
    "publish": "pnpm -r publish --access public --no-git-checks",
    "start": "pnpm run dev",
    "format": "biome check --write",
    "lint": "biome lint --write --unsafe",
    "types": "tsc --noEmit",
    "deps": "pnpm exec knip --production",
    "test": "pnpm --color -r run test",
    "pretest": "pnpm run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.2",
    "@changesets/cli": "^2.29.5",
    "@types/node": "^24.1.0",
    "jsr": "^0.13.4",
    "knip": "^5.62.0",
    "typescript": "^5.8.3",
    "unbuild": "^3.6.0"
  },
  "volta": {
    "node": "20.18.1"
  }
}
